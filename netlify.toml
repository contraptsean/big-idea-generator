# Netlify configuration for Big Idea Radar dashboard
# The build script copies digests/ into frontend/ and generates manifest.json.

[build]
  command = "python build_manifest.py"
  publish = "frontend/"
